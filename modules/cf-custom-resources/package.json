{
  "name": "cf-custom-resources",
  "config": {
    "SRC_DIR": "./src"
  },
  "scripts": {
    "prebuild": "yarn install && (cd $npm_package_config_SRC_DIR && yarn install --production)",
    "build": "yarn test",
    "postbuild": "node upload-lambda",
    "deploy": "cd ops && kumo deploy-module --verbose",
    "destroy": "cd ops && kumo destroy-module --verbose"
  },
  "devDependencies": {
    "directory-s3uploader": "file:../../local-npm/directory-s3uploader"
  }
}
