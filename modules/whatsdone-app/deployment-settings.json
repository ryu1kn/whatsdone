{
    "moduleName": "whatsdone-app",
    "config": {
        "script": "echo {}"
    },
    "tasks": [
        {
            "id": "build-lambda-app",
            "type": "custom",
            "run": {
                "script": "./build.sh"
            }
        },
        {
            "id": "package-lambda",
            "type": "custom",
            "run": {
                "script": "kumo upload-lambda --build-number $BUILD_NUMBER --region $KUMO_TASK_REGION --upload-bucket lambda-packages-cg3j2bu2 --output $KUMO_TASK_OUTPUTS_FILE"
            },
            "outputsName": "packages"
        }
    ]
}
